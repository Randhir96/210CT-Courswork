def reverseWords(input): # function defined with 1 argument.
    inputLength = len(input) 
    if inputLength == 1:
         return input
    return [input[-1]] + reverseWords(input[:-1])
 
inputList = ["this", "is", "awesome"] # array containing words.

print(reverseWords(inputList)) # prints function.
